# Tushare API 注册与使用指南

## 📋 什么是 Tushare？

Tushare 是一个专业的金融数据平台，提供股票、指数、期货、基金等各类金融数据接口。

## 🔑 注册步骤

### 1. 访问注册页面
访问：**https://tushare.pro/register**

### 2. 填写注册信息
- 手机号或邮箱
- 设置密码
- 获取并输入验证码

### 3. 获取 Token
注册成功后：
1. 登录账户：https://tushare.pro/user/index
2. 进入"接口"页面
3. 复制你的 **Token**（格式类似：`abcdef1234567890abcdef1234567890abcdef12`）

### 4. 积分说明
- **免费用户**：有一定的基础积分（约120积分）
- **基础功能**：获取日线数据、基本面数据通常需要120积分
- **积分获取**：
  - 完成实名认证
  - 邀请好友
  - 充值购买

### 5. 设置 Token
将 Token 设置到环境变量或配置文件中：

#### 方式1：环境变量（推荐）
```bash
# Windows PowerShell
$env:TUSHARE_TOKEN="your_token_here"

# Windows CMD
set TUSHARE_TOKEN=your_token_here

# Linux/Mac
export TUSHARE_TOKEN="your_token_here"
```

#### 方式2：创建配置文件
创建 `.env` 文件（项目根目录）：
```
TUSHARE_TOKEN=your_token_here
```

#### 方式3：代码中直接设置（不推荐用于生产环境）
```python
import tushare as ts
ts.set_token('your_token_here')
```

## 📚 常用数据接口

### 1. 股票基本信息
```python
pro = ts.pro_api()
# 获取股票基本信息
df = pro.stock_basic(exchange='', list_status='L', fields='ts_code,symbol,name,area,industry')
```

### 2. 日线行情数据
```python
# 需要120积分
df = pro.daily(ts_code='000001.SZ', start_date='20230101', end_date='20231231')
```

### 3. 周线行情数据
```python
# 需要120积分
df = pro.weekly(ts_code='000001.SZ', start_date='20230101', end_date='20231231')
```

### 4. 财务数据（EPS等）
```python
# 需要120积分
df = pro.fina_indicator(ts_code='000001.SZ', start_date='20230101', end_date='20231231')
# 包含 eps（每股收益）、roe（净资产收益率）等
```

## 🔍 查找股票代码

### 平安银行
- Tushare代码：`000001.SZ` （深交所）
- 或使用 `pro.stock_basic()` 查询

### 其他常用股票
- 腾讯控股：`00700.HK`（港股）
- 招商银行：`600036.SH`（上交所）
- 中国平安：`601318.SH`（上交所）

## ⚠️ 注意事项

1. **调用频率限制**：
   - 免费用户：每分钟约200次
   - 注意控制调用频率，避免被封

2. **数据更新频率**：
   - 日线数据：交易日收盘后更新
   - 财务数据：财报发布后更新

3. **积分要求**：
   - 基础数据查询：120积分
   - 高级数据：可能需要更多积分

4. **数据质量**：
   - Tushare数据质量较高，但建议做数据校验
   - 注意处理停牌、复权等情况

## 📖 官方文档

- 官网：https://tushare.pro/
- 接口文档：https://tushare.pro/document/2
- API文档：https://tushare.pro/document/1

## 💡 快速测试

安装 tushare：
```bash
pip install tushare
```

测试连接：
```python
import tushare as ts
ts.set_token('your_token_here')
pro = ts.pro_api()

# 测试查询
df = pro.stock_basic(limit=5)
print(df)
```

如果返回数据，说明配置成功！

